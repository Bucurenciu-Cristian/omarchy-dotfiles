# ============================================================================
# Omarchy Tmux Theme: cobalt2
# PowerKit Theme: tokyo-night (night)
# ============================================================================
#
# Theme-specific settings only. Personal preferences (plugins, position, etc.)
# are in the main tmux.conf.
#
# ============================================================================

# PowerKit Plugin
set -g @plugin 'fabioluciano/tmux-powerkit'

# Theme Configuration (theme-specific)
set -g @powerkit_theme 'tokyo-night'
set -g @powerkit_theme_variant 'night'

# ============================================================================
